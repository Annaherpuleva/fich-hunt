export const DEFAULT_FEED_PERIOD = 7 * 24 * 60 * 60; // 7 days until becomes victim
export const DEFAULT_MARK_PLACEMENT_WINDOW_SECONDS = 24 * 60 * 60; // 24 hours
export const DEFAULT_MARK_EXCLUSIVITY_SECONDS = 30 * 60; // 30 minutes (fallback, реальное окно берём с ончейна)
export const DEFAULT_HIGH_RATE_THRESHOLD_SECONDS = 3 * 60 * 60; // 3 hours

// export const FEED_PERIOD = DEFAULT_FEED_PERIOD;
// export const MARK_PLACEMENT_WINDOW_SECONDS = DEFAULT_MARK_PLACEMENT_WINDOW_SECONDS;
// export const MARK_EXCLUSIVITY_SECONDS = DEFAULT_MARK_EXCLUSIVITY_SECONDS;
// export const HIGH_RATE_THRESHOLD_SECONDS = DEFAULT_HIGH_RATE_THRESHOLD_SECONDS;

export const FEED_PERIOD = 4 * 60; // 7 days until becomes victim
export const MARK_PLACEMENT_WINDOW_SECONDS = 2 * 60; // 24 hours
export const MARK_EXCLUSIVITY_SECONDS = 60; // 30 minutes (fallback, реальное окно берём с ончейна)
export const HIGH_RATE_THRESHOLD_SECONDS = 60; // 3 hours